package com.vladproduction.file_system;

/**
 * Component: An interface or abstract class defining operations that can be performed on both files and directories,
 * such as display(), getSize(), etc.
 * */
public interface FileSystemComponent {

    void display();
    int getSize();

}
