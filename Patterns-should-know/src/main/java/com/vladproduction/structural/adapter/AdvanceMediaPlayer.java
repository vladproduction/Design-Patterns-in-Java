package com.vladproduction.structural.adapter;

// Adapter interface: the interface that needs adapting
public interface AdvanceMediaPlayer {

    void playMp4(String fileName);
    void playVlc(String fileName);

}
